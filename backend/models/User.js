// User model (to be implemented in Phase 2)
// Fields: id, name, email, password, userType (buyer/seller/staff), createdAt, updatedAt

module.exports = {
  // User creation, retrieval, update, delete operations
};
